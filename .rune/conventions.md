# Detected Conventions

> Auto-generated by rune:onboard on 2026-04-07. Project is in planning phase — no source code exists yet.
> Conventions below are **prescribed** from planning docs, not detected from actual code.

## Language & Runtime
- TypeScript throughout (frontend + backend + workers)
- Node.js runtime for backend services

## Naming Patterns (prescribed)
- Files: kebab-case (e.g., `session-manager.ts`, `ba-agent.ts`)
- TypeScript interfaces: PascalCase with `I` prefix avoided (e.g., `BAInput`, `BAOutput`)
- Zod schemas: PascalCase matching the interface they validate
- Agent jobs: `{agentName}:{action}` format (e.g., `ba:generate-spec`)
- Event bus channels: `{domain}:{event}` format (e.g., `session:decision-made`)

## Module Structure (planned)
- Monorepo with Turborepo: `apps/` for deployable services, `packages/` for shared code
- Shared types in `packages/shared` — imported by all apps
- DB schema in `packages/db` (Drizzle ORM) — never import db directly from frontend

## API Pattern
- REST for CRUD + SSE for metric pushes
- WebSocket for real-time chat (PO ↔ Orchestrator)
- Hono framework — route handlers use typed middleware chain

## Error Handling (prescribed)
- All agent outputs validated with Zod before use
- Retry pattern: max 2 retries with format reminder on validation failure
- Circuit breaker: 5 consecutive failures → pause agent, flag to PO
- BullMQ jobs: configure `attempts: 3` with exponential backoff

## State Management (prescribed)
- **UI state**: Zustand (frontend only)
- **Session/scenario state**: XState v5 state machine (orchestrator)
- **Persistent state**: PostgreSQL + JSONB for session checkpoints
- **Cache**: Redis for hot session data (TTL: 1 hour)

## LLM Usage Conventions
- Complex reasoning agents (BA, Designer, Dev): Claude Sonnet 4.6
- Simple/reactive agents (Stakeholder, Customer, Orchestrator routing): Claude Haiku 4.5
- All LLM outputs MUST be validated with Zod schema before use
- System prompts stored as separate `.prompt.ts` files next to agent

## Scenario Format
- YAML files with JSON Schema validation at load time
- Schema: `id`, `domain`, `type`, `difficulty`, `decision_tree` (DAG), `scoring`, `outcomes`
- Decision trees: DAG of `DecisionNode` with typed `Option[]` and `SideEffect[]`

## Testing (planned)
- Unit tests for: scenario parser, scoring engine, Zod validators
- Integration tests for: agent input/output contracts, orchestrator state transitions
- No E2E (Playwright) until Phase 2
